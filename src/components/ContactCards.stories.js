import ContactCards from "./ContactCards";

export default {
  title: "Example/ContactCards",
  component: ContactCards,
  parameters: { layout: "centered" },
};

export const contactCards = () => <ContactCards />;
